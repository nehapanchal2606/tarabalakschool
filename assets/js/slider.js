$(document).ready(function () {
	// var slider = $('.masthead-slider');
	// $(slider).addClass('owl-carousel owl-theme');
	$('.masthead-slider').owlCarousel({
		loop: true,
		margin: 10,
		nav: false,
		items: 1,
	});
});
